# إصلاح القراءة وRSS — سجل التنفيذ

## المرجع وحماية العمل المحلي

- تقرير المستخدم: `READING_RSS_AUDIT.md` وحزمة `reading-audit-evidence.zip`.
- مرجع الإصلاح: `origin/main@dd90e686`؛ العمل في `/Users/amer/ulf-reading-repairs` (worktree معزول).
- النسخة المحلية `/Users/amer/unified-life-forge` لها HEAD أقدم وتغييرات غير ملتزمة واسعة. لم تُحذف أو تُستبدل، ولا يُستخدم stash عليها.
- لا طلبات إنتاج ولا تعديل بيانات مستخدمين أو نشر دوال خلال التحقق المحلي.

## المرحلة الأولى — قيد التنفيذ

| النتائج | نطاق الإصلاح | الحالة |
|---|---|---|
| R05/R06/R07 | عزل الحسابات، hydration قابل لإعادة المحاولة، مصالحة التغييرات وفشل المزامنة، المصادر الفارغة | قيد التنفيذ؛ لا اعتماد قبل مراجعة اختبارات السباقات والحفظ |
| R08 | لا إعلان تخزين أو تثبيت validators بعد فشل كتابة المقالات | قيد التنفيذ |
| R12 | دمج مستقل للنص والوصف والصورة ومعرض الصور، منع استبدال الجسم بمقتطف | اختبارات الوحدة 4/4 ناجحة بعد إعادة إنتاج الفشل؛ بانتظار بوابة المشروع |
| R13 — جزئي | مطابقة أسماء معاملات التتبع بالكامل بدلاً من حذف كل ما يبدأ بـref ونظائره | اختبارا الهوية ناجحان؛ parsing XML وmissing links لم يُعالجا بعد |
| R18/R19/R23 | الأصل الكامل للترجمة، منع رد قديم من الكتابة فوق مقال جديد، نتائج فشل صادقة وتزامن محدود | قيد التنفيذ |
| R20/R21/R22 | إيقاف الصوت عند تبدل المقال، تفضيلات الإشعارات الحية، حفظ آخر مقال صالح عند فشل الاستخراج | قيد التنفيذ |

### حدود إصلاح الدمج

مقارنة النص fallback تحفظ النسخة الأكثر نصاً مقروءاً مع استبعاد script/style/template وnbsp، وليست تقييماً تحريرياً ولا بديلاً عن provenance للاستخراج. لا تعديل لتعقيم HTML أو صلاحيات الروابط بهذا الإصلاح.

## المراحل التالية

1. **عقود ingestion/detail:** R01/R02/R03/R04/R14 — دفعات كاملة، قراءة بعد حفظ المصدر، detail من القاعدة قبل استخراج الناشر، المفضلات القديمة، هوية المصدر.
2. **الاعتمادية والموارد:** R09/R10/R11/R15/R17 — حدود قراءة متدفقة ومهلة شاملة، ميزانية الاستخراج، retry حسب نوع الخطأ، نطاق إلغاء لكل عملية، إحصاءات cron صادقة.
3. **اتساق المنتج:** بقية R13 وR16 — XML/الهوية، واجهة cloud-only لا تعد بحفظ أوفلاين غير موجود.
4. **منع التراجع:** اختبارات integration للـhook وواجهات الخادم وسباقات الوقت، Deno حيث أمكن، واختبار smoke محلي. اختبارات characterization المرفقة التي تتوقع السلوك الخاطئ ليست دليلاً على نجاح الإصلاح.

## بوابة التسليم

- اختبارات القراءة المركزة + مراجعة diffs.
- `bun run typecheck` مستقل، إذ إن `verify` في هذا المرجع يشغّل الاختبارات فقط.
- اختبارات المشروع كاملة + build + lint للملفات المعدلة، مع فصل العوائق السابقة إن ظهرت.
- لا commit/push قبل اكتمال البوابة؛ لا ادعاء نشر إنتاج قبل تحقق منفصل.
